[Unit]
Description=Seed DankMaterialShell settings defaults
After=graphical-session.target dms.service

[Service]
Type=oneshot
Environment=PATH=/usr/bin:/usr/local/bin
ExecStart=/usr/bin/dms ipc settings set customPowerActionLogout "killall -s SIGTERM labwc"

[Install]
WantedBy=default.target
